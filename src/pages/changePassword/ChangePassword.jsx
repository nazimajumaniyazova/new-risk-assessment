import { Preview } from '../auth/components/Preview/Preview';
import ChangePasswordForm from '../auth/components/changePasswordForm/ChangePasswordForm';

const ChangePassword = () => {
  return (
    <Preview>
      <ChangePasswordForm />
    </Preview>
  );
};

export default ChangePassword;
